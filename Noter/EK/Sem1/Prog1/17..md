```java
import org.junit.jupiter.api.Test;  
  
import static org.junit.Assert.assertEquals;  
  
class BmiTest {  
  
    @Test  
    void calculate() {  
        //Arrange  
        Bmi bmi = new Bmi();  
        //Act  
        double test = bmi.calculate(2, 78);  
        //Assert  
        assertEquals(19.5, test, 0.0);
  
    }  
  
    @Test  
    void calculateForBjarne() {  
        //Arrange  
        Bmi bmi = new Bmi();  
        //Act  
        double test = bmi.calculate(1.8, 77);  
        //Assert  
        assertEquals(23.76543209876543, test, 0.0);  
  
    }  
  
    @Test  
    void isOverForBmi26(){  
        Bmi bmi = new Bmi();  
        double bmiOver = 24;  
  
        boolean result = bmi.isOver(bmiOver);  
  
        assertEquals(false, result);  
    }  
  
    @Test  
    void isNormal(){  
        Bmi bmi = new Bmi();  
        double bmiOver = 20;  
  
        boolean result = bmi.isNormal(bmiOver);  
  
        assertEquals(true, result);  
    }  
}
```

